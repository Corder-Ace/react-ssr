const config = {
    //启动端口
    port: 3000,

    database: {
        DATABASE: 'communityl',
        USERNAME: 'root',
        PASSWORD: '949223',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config
